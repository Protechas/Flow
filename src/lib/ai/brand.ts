/**
 * Flow's AI assistant has one name everywhere it speaks: Eddy — an eddy is a
 * current inside a flow. Use this constant in every user-facing AI surface so
 * the name changes in exactly one place. (Client-safe: no server imports.)
 */
export const AI_NAME = "Eddy";
